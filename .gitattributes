# ./bindings is generated by ts-rs, which always writes LF. Pin it so a regen on Windows does not
# rewrite 193 files as a line-ending-only diff, and so the CI drift gate compares content only.
bindings/*.ts text eol=lf
